# Single compose for dev and tests (PHP + Node for Vite/TS)
name: twig-inspector-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twig-inspector-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: ["tail", "-f", "/dev/null"]
    networks:
      - twig-inspector-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  twig-inspector-bundle-network:
    driver: bridge
