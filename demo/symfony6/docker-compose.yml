services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twig-inspector-symfony6-php
    working_dir: /app
    ports:
      - "${PORT:-8001}:443"
    volumes:
      - .:/app
      # Mount bundle root so path repo symlink and template/code changes are visible
      - ../..:/var/twig-inspector-bundle
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - APP_ENV=dev
    networks:
      - twig-inspector-demo-network

volumes:
  composer-cache:
    driver: local

networks:
  twig-inspector-demo-network:
    driver: bridge
