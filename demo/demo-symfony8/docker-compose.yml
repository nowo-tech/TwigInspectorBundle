services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twig-inspector-demo-symfony8-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: php-fpm
    networks:
      - twig-inspector-demo-network

  nginx:
    image: nginx:alpine
    container_name: twig-inspector-demo-symfony8-nginx
    ports:
      - "${PORT:-8001}:80"
    volumes:
      - ./public:/var/www/html/public:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    networks:
      - twig-inspector-demo-network

volumes:
  composer-cache:
    driver: local

networks:
  twig-inspector-demo-network:
    driver: bridge

