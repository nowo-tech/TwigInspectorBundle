services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: twig-inspector-symfony8-php
    working_dir: /app
    volumes:
      - .:/app
      # Mount bundle root so the path repo symlink works and template/code changes are visible
      - ../..:/var/twig-inspector-bundle
      - composer-cache:/root/.composer/cache
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      # Ensure dev environment so the bundle (and its console commands) is loaded
      - APP_ENV=dev
    command: php-fpm
    networks:
      - twig-inspector-demo-network

  nginx:
    image: nginx:alpine
    container_name: twig-inspector-symfony8-nginx
    ports:
      - "${PORT:-8001}:80"
    volumes:
      - ./public:/var/www/html/public:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
    networks:
      - twig-inspector-demo-network

volumes:
  composer-cache:
    driver: local

networks:
  twig-inspector-demo-network:
    driver: bridge

