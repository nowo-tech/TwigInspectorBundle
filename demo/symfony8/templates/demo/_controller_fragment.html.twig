<div class="info-box mt-3">
    <strong>Fragment via <code>{{ '{{ render(controller(...)) }}' }}</code></strong>
    <p class="mb-0 mt-2">This block is rendered by a sub-request. In the Twig Inspector profiler, open the <strong>Controllers</strong> tab to see the main controller and this fragment controller.</p>
</div>
